
<% unless flash.empty? %>
  <script type="text/javascript">
    <% flash.each do |f| %>
    <% type = f[0].to_s.gsub('alert', 'warning').gsub('notice', 'success').gsub('error', 'error') %>
    toastr['<%= type %>']('<%= f[1] %>');
    <% end %>
  </script>
<% end %>
